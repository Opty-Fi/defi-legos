import BTC_TOKENS from "./btc_tokens";
import PLAIN_TOKENS from "./plain_tokens";
import REWARD_TOKENS from "./reward_tokens";
import WRAPPED_TOKENS from "./wrapped_tokens";
import MULTI_ASSET_TOKENS from "./multi_asset_tokens";

const tokens = {
  BTC_TOKENS,
  PLAIN_TOKENS,
  REWARD_TOKENS,
  WRAPPED_TOKENS,
  MULTI_ASSET_TOKENS,
};
export default tokens;
