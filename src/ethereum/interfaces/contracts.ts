import ETHGatewayAbi from "./ETHGateway.json";
import HarvestCodeProviderAbi from "./HarvestCodeProvider.json";

const contracts = {
    ETHGateway: { abi: ETHGatewayAbi },
    HarvestCodeProvider: { abi: HarvestCodeProviderAbi }
};

export default contracts;